services:
  web:
    build: .
    restart: always
    ports:
      - "3000:3000"

    volumes:
      - .:/app

    environment:
      NODE_ENV: development

    env_file:
      - .env.local

    command: npm run dev